# Productization Phase 1 Checklist

- [x] Add MIT LICENSE at repo root.
- [x] Add CHANGELOG.md with an entry for the current release.
- [x] Add `--version` flag in `ralph-codex.sh` and output a version string.
- [x] Add JSON schema for `.ralph/config.json` at `schema/config.schema.json`.
- [x] Add `validate` command to `ralph-codex.sh` to validate config against the schema.
- [x] Add GitHub Actions CI workflow that runs validation and a dry-run smoke check.
